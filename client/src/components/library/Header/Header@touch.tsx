import * as React from "react";

interface iHeaderProps {
    menuExpanded: boolean
}

export const Header: React.FunctionComponent<iHeaderProps> = (props) => {
    return <div>Мобильный хедер</div>;
};